services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always

  # Mobile app is not containerized in this setup as it's a Flutter project
  # which runs on physical devices/emulators.
